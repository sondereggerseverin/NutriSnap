name: Build APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: NutriSnap-debug
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 14

      # ── Optional: Release APK (signed) ───────────────────────────────────────
      # Uncomment and add secrets KEYSTORE_BASE64, KEYSTORE_PASS, KEY_ALIAS, KEY_PASS
      # in GitHub Settings → Secrets → Actions to enable signed release builds.
      #
      # - name: Decode keystore
      #   run: |
      #     echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > app/release.keystore
      #
      # - name: Build Release APK
      #   env:
      #     KEYSTORE_PATH: release.keystore
      #     KEYSTORE_PASS: ${{ secrets.KEYSTORE_PASS }}
      #     KEY_ALIAS:     ${{ secrets.KEY_ALIAS }}
      #     KEY_PASS:      ${{ secrets.KEY_PASS }}
      #   run: ./gradlew assembleRelease --no-daemon
      #
      # - name: Upload Release APK
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: NutriSnap-release
      #     path: app/build/outputs/apk/release/*.apk
      #     retention-days: 30
